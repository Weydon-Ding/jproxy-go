# v0.1.0 排期计划

## 版本定位

`v0.1.0` 建议定位为：

```text
Core Proxy MVP
```

目标是发布第一个外部可部署、可真实接入测试的基础代理版本。

该版本重点验证 Go 版迁移路线、基础代理链路、缓存、XML 合并/裁剪、配置和 Docker 部署能力。

## 推荐周期

```text
约 2 周 / 10 个工作日
```

## v0.1.0 范围

### 必须包含

- 核心 Jackett/Prowlarr 代理。
- Sonarr/Radarr 路由。
- 基础搜索扩展。
- XML 合并、计数与裁剪。
- 结果缓存。
- offset 分页缓存。
- 基础配置能力。
- 健康检查。
- Docker 构建与运行。
- README 部署说明。
- release note。

### 明确不包含

- 完整规则格式化引擎。
- 标题库 / 别名库。
- SQLite 存储。
- Java DB 完整兼容。
- Web 管理后台。
- 登录 / JWT。
- Downloader 集成。
- 多用户、多实例管理能力。

## 推荐版本路线

```text
v0.1.0 = Core Proxy MVP
v0.2.0 = Rule Formatting
v0.3.0 = Title Library / Alias / SQLite
v0.4.0+ = Admin API/UI、登录、下载器集成等
```

## 推荐排期

| 时间 | 里程碑 | 目标 |
|---|---|---|
| T+1 到 T+2 | M1 测试补齐 | 代理主链路、XML、缓存、配置测试 |
| T+3 到 T+4 | 文档与部署补齐 | README、Docker、环境变量、健康检查说明 |
| T+5 到 T+7 | 集成验证 | Jackett/Prowlarr、Sonarr/Radarr 接入测试 |
| T+8 到 T+9 | release 冻结 | 创建 `release/v0.1.0`，只接受 bugfix |
| T+10 | 发布 | 合并 main、打 tag、main 合并回 dev |

## 里程碑细化

### M1：核心代理链路测试补齐

推荐分支：

```text
feature/proxy-mvp-tests
```

测试范围：

- 路由路径：
  - `/sonarr/jackett/*`
  - `/sonarr/prowlarr/*`
  - `/radarr/jackett/*`
  - `/radarr/prowlarr/*`
- Query 参数透传。
- 上游错误处理。
- XML 合并。
- XML 裁剪。
- 空 XML / 无 item XML。
- offset 分页缓存。
- 基础结果缓存。
- 基础搜索词扩展。
- 配置默认值与环境变量覆盖。

建议测试文件：

```text
internal/proxy/format_test.go
internal/proxy/xml_test.go
internal/proxy/server_test.go
internal/config/config_test.go
```

### M2：部署与文档完善

推荐分支：

```text
feature/runtime-config
```

范围：

- README 快速启动。
- 环境变量说明。
- Docker 使用说明。
- Sonarr/Radarr 接入路径说明。
- `/health` 说明。
- 已知限制。
- v0.1.0 release note 草案。

### M3：集成验证

范围：

- Jackett 搜索验证。
- Prowlarr 搜索验证。
- Sonarr 通过 jproxy-go 搜索验证。
- Radarr 通过 jproxy-go 搜索验证。
- offset/limit 分页验证。
- 缓存命中验证。
- Docker 容器运行验证。
- 上游不可达异常验证。

### M4：预发布

分支：

```text
release/v0.1.0
```

规则：

- 冻结功能。
- 只接受 bugfix。
- QA 执行回归清单。
- 准备 release note。

### M5：正式发布

流程：

```text
release/v0.1.0 -> main
tag v0.1.0
main -> dev
```

## 验收标准

### 代码级验收

必须通过：

```bat
go test ./...
go build ./cmd/jproxy
```

建议通过：

```bat
go vet ./...
golangci-lint run
```

注意：`go test ./...` 通过但没有测试文件时，不能视为质量充分。

### 功能验收

- Sonarr/Radarr 路由能正确转发到 Jackett/Prowlarr。
- Query 参数完整透传。
- 上游异常时返回合理错误，不导致进程崩溃。
- 多次搜索结果可合并为可用 XML。
- `limit` / `offset` 场景行为正确。
- 缓存 TTL 生效。
- `/health` 返回 `ok`。

### Docker 验收

示例命令：

```bat
docker build -t jproxy-go:v0.1.0 -f deployments/docker/Dockerfile .
```

运行后访问：

```text
http://127.0.0.1:8117/health
```

期望返回：

```text
ok
```

### 发布验收

- `release/v0.1.0` 分支创建。
- README 更新。
- Docker 构建通过。
- 本地二进制构建通过。
- 至少一组 Jackett 或 Prowlarr 实测通过。
- 至少一组 Sonarr 或 Radarr 接入测试通过。
- 已知限制写入 release note。
- `main` 打 tag：`v0.1.0`。
- `main` 合并回 `dev`。

## 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 当前测试覆盖不足 | 发布质量不可控 | 优先补 M1 测试 |
| 规则格式化范围过大 | 拖慢 v0.1.0 | 推迟到 v0.2.0 |
| XML 字符串处理较朴素 | 兼容性风险 | 用典型 Jackett/Prowlarr XML 样例覆盖 |
| Java 版行为兼容性不确定 | 用户预期偏差 | release note 写明已知限制 |
| Docker 访问宿主机服务差异 | 部署踩坑 | README 区分 Windows/macOS/Linux 说明 |

## 下一步行动

1. 确认 `v0.1.0 = Core Proxy MVP`。
2. 提交当前规划文档。
3. 从 `dev` 创建分支：

```text
feature/proxy-mvp-tests
```

4. Engineer 补齐测试。
5. Architect 轻量 review 代理链路、XML 处理和缓存边界。
6. QA 准备 v0.1.0 回归清单。

## 角色介入建议

### Architect

需要轻量介入：

- 确认 v0.1.0 范围收敛。
- Review XML 字符串处理是否可接受。
- Review offset cache 边界。
- 为 v0.2.0 规则格式化预留演进建议。

### Engineer

必须介入：

- 补单元测试。
- 补 `httptest` 集成测试。
- 修复测试暴露的问题。
- 验证 Docker 构建。
- 补 README 技术细节。

### QA

必须介入：

- Jackett/Prowlarr 搜索验证。
- Sonarr/Radarr 接入验证。
- offset/limit 分页验证。
- 缓存验证。
- Docker 运行验证。
- 异常场景验证。

## 工作流改进记录

subagent 的上下文生命周期只有单次调用，关键产出不能只留在聊天中。

后续要求：

```text
PM / Architect / Engineer / QA 的关键产出，默认必须写入或更新项目内文档、代码或测试文件。
```

在团队知识库搭建前，以仓库内产出为准。
